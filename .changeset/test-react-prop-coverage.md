---
'@ghs-hazard-pictograms/react': patch
---

Add test coverage for `title`, `description`, `aria-label`, `width`, and `height` props on React pictogram components, including XSS regression tests and a duplicate-attribute regression test for `width`/`height`.
