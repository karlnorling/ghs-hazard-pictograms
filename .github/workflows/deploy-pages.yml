name: Deploy Storybook to GitHub Pages

on:
  push:
    branches:
      - main

# Allow one concurrent deployment; cancel in-progress runs when a new push arrives
concurrency:
  group: pages
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  deploy:
    name: Build and deploy Storybook
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Enable Corepack (Yarn 4)
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Compile workspace packages
        run: yarn compile

      - name: Build Storybook
        run: yarn workspace @ghs-storybook/app build-storybook
        env:
          # Set the Vite base URL to the GitHub Pages sub-path so assets load correctly
          STORYBOOK_BASE_PATH: /${{ github.event.repository.name }}/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: apps/storybook/storybook-static
